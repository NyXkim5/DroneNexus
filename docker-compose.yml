version: "3.8"
services:
  overwatch:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OVERWATCH_ENV=production
    volumes:
      - ./config:/app/config
    restart: unless-stopped
